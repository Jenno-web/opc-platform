# OPC 供需平台 —— 项目介绍

体验链接：https://opc-platform-frontend.vercel.app/
（手机、电脑浏览器直接打开即可，会自动登录演示账号）

## 这是什么

把之前的 OPC 产品设计资料（信息架构文档、AI 能力风险矩阵、Figma 低保真原型）做成了一版可以实际操作的全栈 Demo，用来验证这套设计方案是否真的能落地成产品，而不只是停留在设计稿阶段。后续又对照 Figma 里的真实设计稿（颜色、布局、文案）重新核对了一遍视觉和交互细节。

## 用了什么技术

| 层 | 技术选型 |
|---|---|
| 前端 | uniapp + Vue3 + TypeScript（一套代码可以同时出 H5 网页 / 小程序 / App，目前部署的是 H5 版本） |
| 后端 | NestJS + TypeScript（Node.js 框架），REST API + WebSocket 实时推送 |
| 数据库 | PostgreSQL，用 Prisma 做 ORM |
| AI 能力 | 抽象出一套"可插拔"的 AI 接口，现在跑的是规则模拟，接一个大模型 API Key 就能切换成真实 AI（比如接 Claude） |
| 部署 | 前端托管在 Vercel，后端 + 数据库托管在 Railway，代码开源管理在 GitHub |

## 是怎么做的

1. **先读懂设计资料**：逐份分析了 IA 文档、AI 能力风险矩阵、产品研究报告，梳理出核心信息架构（发现 / 任务 / 发布 / 信息 / 我的五大模块）和 AI 能力如何嵌入各个页面
2. **搭数据模型**：把用户、项目、任务、消息、待办、知识库这些实体设计成数据库表结构
3. **写后端接口**：五大业务模块 + 六类 AI 能力（推荐/生成/总结/分析/提取/工作流）对应的 API，每次 AI 输出都会记录下来，方便追溯依据、支持用户确认修改
4. **写前端页面**：把设计文档里的页面结构和交互流程实现成真实可点击的界面
5. **对照 Figma 真实设计稿重做**：拿到 Figma 文件后，发现之前凭文字描述做的版本跟真实设计有出入（配色、导航命名、部分页面结构），逐屏核对后重新对齐
6. **部署上线**：把本地能跑的版本部署到真实服务器，这样任何人打开链接都能直接体验，不用配置开发环境

## 我在这个过程里做了什么

具体代码是在 AI 编程工具（Claude Code）辅助下写的，我的角色更像是主导整个项目的人：

- 读完设计资料后判断"应该做成真实可跑的产品"，而不是停在文档和原型阶段
- 每一步的关键决策都是我拍板的：技术方向、MVP 先做到什么程度、AI 要不要接真实大模型、发现视觉和设计稿对不上之后要不要推翻重做（后来选了最全的返工方案）、最后用部署上线还是打包发源码
- Figma、GitHub、Railway、Vercel、Neon 这些账号都是我自己注册和授权的，这一步别人替代不了
- 部署上线这块自己蹚了不少坑：Railway 报错好几轮（配置文件路径不对、Node 版本不匹配、构建缓存冲突、环境变量没保存、部署后启动崩溃），每次都是我在后台里操作、截图、看报错信息，一步步排查着改对的

## 需要说明的地方

- AI 相关功能（推荐、生成草稿、总结、匹配分析等）目前用的是规则模拟，不是真实调用大模型，因为还没有申请 API Key
- 语音房功能只做了"谁在场"的实时状态展示，没有做真实语音通话（需要额外的音视频服务）
- 短信登录验证码是模拟的，没接真实短信服务商
- 小程序/App 版本代码层面已经能编译出来，但正式发布需要微信开发者账号 / App Store 账号

这几项都是需要额外的账号、密钥或者专门的基础设施才能做真，目前用规则模拟的方式把交互流程和产品逻辑先跑通了。
